-UCR shows sending account_id for getMapSuggestions() request to the recommendation system - should be changed to sending filters instead
-UCR shows sending source & target in reroute() request to the navigation system - should we do this or just use the already-computed new path
-UCR doesn't use the external systems, instead just uses DB's
-add to the successful API tests so they are not only checking the return status
-add in some baseline filters to amenity router endpoints (e.g. amenity status = open)
  -add in functionality to use binary arithmetic comparison operators for filters (e.g. currentAvailableSlots: {gt: 5})
-add in amenity occupancy sensor BE - provides API to call updateOccupancy on SystemBE -> should call AmenityBE to persist the data
  -still retrieve data from only the AmenityBE
-add in crowd sensor BE - provides API to accept specific hallways and their updates, converts to the CSV format and calls the NavigationBE
-add in InfrastructureBE - mark hallways as closed
-add DBs as backups for the external systems, add in the exception catching mechanism to use them
-add exception handling for amenity retrieval if the nav system is inaccessible